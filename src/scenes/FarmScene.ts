import { Scene } from 'phaser';

export class FarmScene extends Scene {
  constructor() {
    super('FarmScene');
  }

  create(): void {
    // Placeholder — game logic will be added later.
    // This file MUST exist to prevent scene transition crash.
  }
}
