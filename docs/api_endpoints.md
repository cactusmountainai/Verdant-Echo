# API Endpoints Documentation

## Overview
This document describes the RESTful API endpoints for the Farm Management System. All endpoints are served from `/api/v1` and require authentication via JWT tokens in the `Authorization` header.

## Authentication

### Login
